#include <iostream>
#include <stack>
#include <string>
using namespace std;

string PPP[100009]

    int
    main()
{
    int q;
    cin >> q;
    int a;
    stack<string> S;
    for (int i = 1; i <= q; i++)
    {
        cin >> a;
        if (a == 1)
        {
            cin >> PPP[i] S.push()
        };
        if (a == 2)
            cout << S.top() << endl;
        if (a == 3)
            S.pop();
    }
}